# 报告

## 数据采集
- 股票: 完全免费api  
1.属性太多我说不完,也不认识,就认识一个价格
- 评论: 逆向高速api
1.评论的全部信息,包括评论的内容、点赞数、评论时间  
- tips： 逆向的方法我在美团，微博上都使用过，跟网站复杂程度无关，就是好用，没有道理的好用。
  腾讯视频，mooc视频的流式传输(.ts)也可以逆向成功。美团的遇到了使用json混淆技术的地方，  
  找到解码的js代码就好（很弱的混淆，就加了个字符串包裹整个json）。差不多就这样。


## 数据预处理
***特点：完全增量更新***
- 正则过滤
    ```python
    text = re.sub(r'http[s]?://\S+', '', text) # 过滤超链接
    text = re.sub(r'\$\S+\$', '', text) # 过滤股票代码
    text = re.sub(r'@\S+', '', text) # 过滤@用户
    text = re.sub(r'<.*?>', '', text) # 过滤HTML标签
    ```
  - 情感分析
    - 点赞加权
      - 过滤中性评论
        - 取同日评论的平均情感值及其标准差
  - 二次正则过滤
    ```python
    #去除任何非汉字内容
    text = re.sub(r'[a-zA-Z0-9]', '', text)
    text = re.sub(r'[^\w\s]', '', text)
    ```
  - jieba分词(自带去除停用词)
    - 得到词表  
- 按每日聚合
- 合并方式：时间对齐
  - 问题： 股票周六周末不开市，怎么处理？
  如果将多余自然规律添加到数据中让模型学习，会增模型的负担。
  因此，数据预处理和模型架构设计中：将已有的知识嵌入到模型中，
  使得它作为模型学习的公理。  
  但是非常遗憾的：包括但不限于股票，现实事物的逻辑都过于复杂。
  简单的逻辑早早被基础科学发现并总结，并不留给数据科学家来解决。


## 数据分析
